
######################################
# webui
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: webui
  name: webui
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: webui
    spec:
      containers:
      - image: chrch/webui
        name: webui
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: webui
  name: webui
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: webui
  type: NodePort
---

######################################
# worker
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: worker
  name: worker
spec:
  replicas: 1
  template:
    metadata:
      labels:
        run: worker
    spec:
      containers:
      - image: chrch/worker
        name: worker
---

######################################
# hasher
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: hasher
  name: hasher
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: hasher
    spec:
      containers:
      - image: chrch/hasher
        name: hasher
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: hasher
  name: hasher
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: hasher
  type: ClusterIP
---

######################################
# rng
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: rng
  name: rng
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: rng
    spec:
      containers:
      - image: chrch/rng
        name: rng
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: rng
  name: rng
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: rng
  type: ClusterIP
---

######################################
# redis
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: redis
  name: redis
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - image: redis
        name: redis
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis
  name: redis
spec:
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: redis
  type: ClusterIP







